## Summary

<!-- What does this PR do? Keep it brief. -->

## Test plan

<!-- How did you verify your changes? -->
- [ ] `npm test` passes (83+ tests)
- [ ] `npm run lint` passes
- [ ] `npm run build:bundle` succeeds

## Review scope

This PR targets the `modernize-v2` branch. Please review only **changed or new code** in the diff. Pre-existing issues in unchanged files are tracked separately and should not block this PR.
